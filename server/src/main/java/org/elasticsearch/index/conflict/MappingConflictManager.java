package org.elasticsearch.index.conflict;

import java.util.List;

public class MappingConflictManager {

  /*  public static volatile MappingConflictManager instance;

    private List<MappingConflictResolver> resolvers;

    private FailedEventProcessors processors;


    public static MappingConflictManager getInstance() {
        if (instance == null) {
            synchronized (MappingConflictManager.class) {
                if (instance == null) {
                    instance = new MappingConflictManager();
                }
            }
        }
        return instance;
    }

    private MappingConflictManager() {
        resolvers = MappingConflictUtils.initMappingConflictResolvers();
        for (MappingConflictResolver resolver : resolvers) {
            processors.addProcessor().
        }
    }*/
}
